import React from "react";
import Layout from "../components/Layout";
import SkillSet from "./SkillTest";

const index = () => {
  return (
    <Layout>
      <SkillSet />
    </Layout>
  );
};

export default index;
